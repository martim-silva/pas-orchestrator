---

# Backward-compatible wrapper; prefer windows_iis_features.yml or
# pvwa_iis_features.yml for new call sites.
- name: Include shared Windows IIS feature enablement
  ansible.builtin.include_tasks: windows_iis_features.yml

